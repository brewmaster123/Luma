# Luma 0.4 — проверка 12 сентября 2026

Основа: Luma-iPhone-AppleWatch-v0.3.zip, SHA-256 37d5a29c0891f2661abfea2d8601582ee2dcc77a6b3e3b190aa3e5c5c2820c34. Это отчёт о выполненных проверках исходников и макета, не результат сборки iOS.

| Проверка | Результат |
|---|---|
| Ядро Swift 6.0.3, режим Swift 5, Linux | Типы проверены; библиотека LumaCore и все файлы XCTest скомпилированы. |
| Исполнение XCTest | 49 тестов, 0 ошибок, прямой запуск через оболочку. Логи: core-tests.log и core-tests-direct-v0.4.log. |
| Срок остановки | Реальная длительность, граница срока, позднее возобновление без нового отсчёта, сериализация и повреждённые значения. |
| Календарь и миграция | Прошедший одноразовый не переносится; предыдущая/следующая недельная дата; старые JSON, голосовые вложения, интервалы и сортировка. |
| Swift syntax parse | Все файлы Sources разобраны. Это не typecheck UIKit, SwiftUI, HealthKit, AlarmKit или WatchKit. |
| Xcode project | 129 объектов, 83 структурные проверки; новый AlarmStopWindow включён в обе цели. |
| Макет | 320×568, 375×667, 390×844, 430×932, 1440×1000: пустая «Ночь» помещается; справка прокручивается. |
| Взаимодействия | 8 сценариев: справка/фокус; сортировка/широкое нажатие; новый выбор сигналов; импорт; голосовой сон; отмена вложения; голосовой отклик; удаление аудио. |
| Текст справки | Девять абзацев одинаковы в SwiftUI, HTML и REM-HELP-USER-TEXT.md. |
| JavaScript | node --check и браузерные сценарии без pageerror. Использован тестовый микрофон Chromium. |
| Визуальная проверка | Просмотрены главный экран и справка. PNG созданы браузером, не iOS Simulator. |
| GitHub workflow | YAML и 6 bash-блоков разобраны; две копии workflow совпадают. Фактический GitHub запуск не выполнялся. |
| Xcode / устройства | Не запускались. Нативная компиляция и AVFoundation-конвертер остаются проверками macOS/GitHub; Silent/Focus и физический stop — проверками устройств. |

SwiftPM в этой среде завершился SIGILL с диагностикой /proc/stat. При первой попытке запуск XCTest дочерним процессом Python также упал. После прямой компиляции библиотеки и существующих XCTest через swiftc тестовый файл был запущен непосредственно из оболочки и завершился с кодом 0: все 49 тестов прошли. Это не основание писать, что обычная команда swift test здесь прошла. Детали: swift-validation-v0.4.json и swiftPM-v0.4.log.

Браузерный импорт использует Web Audio и IndexedDB; он не проверяет нативный AVAudioConverter, AlarmKit, фоновое исполнение iOS или датчики Watch. Подробности макета: audio-preview-checks.json.

Watch NotificationService сохранён побайтно из ранее подтверждённой рабочей версии: SHA-256 a6897bffa2b13423ae815293cde01f808fce634db24b90a6fddffb80d0666215.

**Не обеспечено и не заявлено:** гарантированный stop при приостановке/закрытии Luma; измеренная точность REM; обнаружение каждого эпизода; отдельная настройка системной вибрации AlarmKit; подписанный IPA. Требование обязательной автоматической остановки в любых условиях остаётся нерешённым.
